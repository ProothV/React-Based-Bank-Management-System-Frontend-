its consisting all important filess like app.js, index.js,app.test,etc. are very importatnt
all other files in comonent where imported in app.js and routing(Linking of the pages are done in this file)
